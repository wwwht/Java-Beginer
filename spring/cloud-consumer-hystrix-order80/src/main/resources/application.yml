server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka

ribbon:
  ReadTimeout: 4000
  ConnectTimeout: 4000
feign:
  hystrix:
    enabled: true